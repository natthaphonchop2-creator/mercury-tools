# Mercury Finance v0.2.1

Mercury Tools v0.2.1 provides the public `mercury-finance` Codex plugin and
local stdio MCP with exactly 19 tools, plus the separately deployed hosted MCP
with exactly 20 tools.

The release assets are reproducible, SHA-256 bound outputs from the reviewed
commit. The annotated `v0.2.1` tag is immutable.

Mercury Tools is an independent open-source project and is not affiliated with
Mercury Technologies, Inc.
